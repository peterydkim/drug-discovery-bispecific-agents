# Output

Run the pipeline and this directory will contain generated results. Each module produces both a markdown report and a structured JSON block.

To generate output: follow the instructions in `orchestrator/prompt.md` or load the orchestrator skill.
